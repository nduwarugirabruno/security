spring:
    application:
        name: security
    datasource:
        url: jdbc:postgresql://localhost:5432/links_security
        username: links
        password: links
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                format_sql: true
        database: postgresql
        open-in-view: true
